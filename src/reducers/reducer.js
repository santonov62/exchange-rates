
export const initialState = { "EURRUB": "71.3846", "USDRUB": "58.059" };

export const reducer = (state, action) => {
    switch (action.type) {
        case 'rates':
            break;
    
        default:
            throw new Error();
    }
}